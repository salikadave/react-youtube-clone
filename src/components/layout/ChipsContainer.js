const chipsContainer = (props) => {
  return <div style={styles.container}>Chips</div>;
};

const styles = {
  container: {
    backgroundColor: "#ccc",
    height: 30,
    borderColor: 'black'
  },
};

export default chipsContainer;
