import VideoPlayer from "../UI/VideoPlayer";

const VideosContainer = (props) => {
  return (
    <div>
      <VideoPlayer />
      Video container
    </div>
  );
};

export default VideosContainer;
